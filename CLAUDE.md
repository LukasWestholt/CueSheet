# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
npm run dev        # Vite dev server on http://127.0.0.1:5173 (host is 127.0.0.1, not localhost — Spotify only accepts loopback-IP redirect URIs)
npm run build      # tsc -b && vite build  → dist/ (also generates the PWA service worker)
npm run preview    # serve the production build
npm run typecheck  # tsc -b (no emit); the build runs this first, so a green build means types pass
```

No linter or CI is configured. Tests run on **Vitest** (`npm test` watch, `npm run test:run` once). **Business logic is what gets tested**: the pure functions (`resolveCallings`, `interpolatePosition`, `beatsForStep`/`buildCallings`, `resolveTrackMeta`, `bpmFromTaps`, `formatClock`/`formatLong`) and the player phase transitions / gap / hold. Most UI is split into thin presentational components (`components/player/*`, `ListScreen`) driven by hooks, so it isn't unit-tested; Spotify network calls aren't either — `usePlayerEngine.test.ts` mocks `src/spotify/api.ts` and drives the engine's two timers with `vi.useFakeTimers()` + `advanceTimersByTimeAsync`. Test files (`*.test.ts`) are excluded from the production app build in `tsconfig.app.json` (so they never reach `dist/`), but they get their own project — `tsconfig.vitest.json` (proper `lib` + `vitest/globals`), referenced from the root `tsconfig.json` — so `tsc -b` and the editor both type-check them. (Without that project the editor fell back to the default lib and flagged TS2583 "cannot find name 'Set'".)

Requires a `.env` (gitignored): `cp .env.example .env` and set `VITE_SPOTIFY_CLIENT_ID`. Without it the app renders a "Setup needed" screen (`IS_CONFIGURED` gate in `src/config.ts`). The Client ID is a public identifier, not a secret.

## The one architectural idea that explains everything

The app is a **client-only, offline-first PWA** — there is no backend of our own. It builds to static assets in `dist/` (with a generated service worker) deployable to any static host, and should keep working offline wherever it can: the shell, track list, and authored callings must not depend on the network. Only the live Spotify control/position calls require connectivity; degrade gracefully when they're unavailable rather than blocking the UI. In practice: the service-worker precache (shell + JS/CSS + icon/manifest + a navigation fallback) loads the app offline, and routines/favorites/calibration come from `localStorage`, so the list and editor work offline. `useOnline` drives an offline banner, gates the metadata/resume fetches, and suppresses their errors while offline; `LoginScreen` disables login offline.

This PWA is a **Spotify Connect remote — it never plays audio itself.** The iPhone running the PWA sends Web-API commands to Spotify's cloud, which controls a *separate* device (an Android tablet running the Spotify app, Premium) that plays audio to a Bluetooth speaker. This exists because Spotify's Web Playback SDK does not work on iOS Safari, so a PWA cannot stream Spotify directly. Every design constraint below follows from this:

- **No backend.** Auth is client-side Authorization Code + PKCE (`src/spotify/auth.ts`), tokens in `localStorage`, refreshed transparently in `getAccessToken()`. The redirect URI is derived from `window.location.origin + '/callback'`, so each deploy origin must be registered in the Spotify dashboard.
- **Position is polled, not streamed.** `GET /me/player` (`getPlaybackState`) returns `progress_ms` at poll time. The engine **interpolates** between polls (`progress_ms + (now − fetchedAt)`, the pure `interpolatePosition` helper in `src/playback/position.ts`) for a smooth display. Expect ~½–1s jitter plus Bluetooth audio latency — the **Sync offset** slider in `PlayerScreen` (persisted to `localStorage`) calibrates the constant lag by shifting the position used for calling resolution. Evaluating this precision is the prototype's open question.
- **Spotify access is behind `src/spotify/api.ts`** (devices, play/pause/seek, playback state) so a different audio source could replace it without touching the UI.

## How the player engine works (`src/hooks/usePlayerEngine.ts`)

This hook is the core and the trickiest file. It runs a phase state machine: `idle → loading → playing → (paused | gap | held | ended)`.

- **Two timers:** a 1s **poller** fetches authoritative state from Spotify; a 100ms **ticker** interpolates the displayed position and runs the gap countdown. Both read mutable **refs** (`phaseRef`, `indexRef`, `autoRef`, `snapshotRef`) so they always see fresh values without being torn down and recreated on every state change — when editing, keep each `setX` paired with its `xRef.current = ` update.
- **Track-end detection** is heuristic (no native "ended" event): the ticker triggers when interpolated position reaches `duration − 500ms`; the poller also triggers if Spotify reports `!isPlaying` within 2s of the end. If we lose that ~500ms pre-empt race, the poller still treats the end as an end (not a hijack/replay) when our last snapshot was within `END_AUTOPLAY_WINDOW_MS` (1.5s) of the end: a **different track** (device auto-advance) or the **same track jumping back to ~0** (`repeat = track` loop, `progressMs < LOOP_RESTART_MS`) both call `enterGapOrEnd`. We deliberately **don't** change the device's repeat/shuffle.
- **Inter-track gap + permanent pause** are both here: on track end, if `autoContinue` is on it enters `gap` (countdown then auto-advance — `gapSeconds` param, `DEFAULT_GAP_SECONDS` = 10s); otherwise `held`. `holdNow()` is the "pause permanently between tracks" button. UI overlays for `gap`/`held` are `components/player/GapOverlay` + `HeldOverlay`, composed by `PlayerScreen`.

## App composition (hooks & screens)

`App.tsx` is a **thin orchestrator**: it owns navigation (the `list ⇄ player ⇄ editor/seed` view switch, deep-link/`popstate` effects, the metadata + resume fetches) and composes focused hooks + screen components. Almost all stateful logic lives in **custom hooks** so App stays scannable:

- `src/hooks/useAuthSession.ts` — the OAuth lifecycle (callback exchange, token auto-refresh, expiry → login), plus the account profile (`getCurrentUser`) behind the **Premium gate**. Takes `{ online, onExpired }`; exposes `{ ready, loggedIn, authError, account, logout }`.
- `src/hooks/useRoutineSources.ts` — the entire composable-source layer (active `tracks`, custom files, enabled sources, server-content cache, first-run seeding, every merge/toggle/reset). `commitTracks` calls back `onListReplaced` so navigation selection resets stay in App.
- `src/hooks/useFavorites.ts`, `src/hooks/useSetlist.ts` — small persisted-state hooks (favorites Set; setlist + derived `sessionTracks`).
- `src/components/ListScreen.tsx` — the whole list view (topbar + account/Premium banner, search/favorites filter, `TrackList`, `SetlistPanel`, `RoutinesManager`, `Settings`); presentational, list-local search state lives here.

`PlayerScreen.tsx` keeps the engine wiring, `meta`/`callings`, `positionSeconds`, and composes presentational pieces under **`src/components/player/`**: `PlayerControls`, `StepTimeline`, `SyncOffsetSlider`, `VolumeSlider`, `CalibrationPanel` (owns tap-tempo state), `GapOverlay`, `HeldOverlay`, `DeviceBanners`, + shared `messages.ts`. The sync offset is its own `src/hooks/useSyncOffset.ts`.

Shared utilities: `src/data/time.ts` (`formatClock` `m:ss` / `formatLong` `h:mm:ss`) is the single source for playback-time formatting; `src/data/storage.ts` wraps `localStorage` with `readJSON`/`writeJSON` plus `readFlag`/`writeFlag` (booleans as '1'/'0') and `readString`/`writeString` — prefer these over raw `localStorage` calls.

## Calling data (the routines)

Steps are authored **musically, not in seconds**. In `src/data/tracks.ts` each track has a `firstBeatSec`, a `bpm` (optional), and `steps: { step, cue, measures }[]` where `measures` is the step's length in 8-counts (halves allowed). The `spotifyUri` values are `REPLACE_ME` placeholders to swap for real track URIs.

**The committed routine set** lives as JSON in `public/playbook-2026.json` (the real 30-track playbook, checked into git). `tracks.ts` keeps only a small `DEFAULT_TRACKS` sample exported as `TRACKS`, used solely as the offline/no-server fallback — the old `import.meta.glob` / gitignored `tracks.local.ts` mechanism was removed.

At **runtime** the active routine list is **materialized** in `localStorage` (`tjf.tracks`, `src/data/tracksStore.ts`) — it's what the app renders and what the editor mutates. On top of it sits a **composable-source** layer — pure merge/remove helpers in `src/data/routineSources.ts`, orchestrated by the `useRoutineSources` hook (see "App composition" above): each routine file — one shipped in the public folder *or* one the user uploaded — is an independently toggleable source, and toggling **merges** that file's tracks into the active list (`mergeTracks`, first-wins dedup by id) or **removes** them (`removeTracksByIds`, keeping any id another enabled source still provides). The set of enabled source keys + a one-time `initialized` flag persist in `tjf.sources`; uploaded files persist (content and all) in `tjf.customFiles`. On **first run** (no `tjf.sources`, no legacy `tjf.tracks`) the public-folder **default** files (manifest entries flagged `"default": true` or named `default*.json` — here `playbook-2026.json`) are auto-enabled and materialized; a pre-existing `tjf.tracks` from before this model counts as already-initialized so defaults aren't merged on top of it. Server-file contents are fetched + validated on demand and cached in memory; `src/data/recommendedImports.ts#loadRecommendedRoutines` still reads the `public/routines.json` manifest. **Disabling every source leaves the active list empty — that's allowed.** The code-defined `DEFAULT_TRACKS` (`tracks.ts`) remain only as the no-server/offline fallback. The **Routines panel** (`RoutinesManager`) shows two sections — **"Routine files on this server"** and **"Custom routine files"** — each row a **Load/Loaded** toggle (custom rows also get a remove ✕); **Upload** adds a file to the custom list *without* loading it (no longer replaces the list); **Export** backs up the active list; **Reset** re-enables just the public defaults; and a **Contribute a routine** modal explains the PR workflow (add a file to `public/` + an entry to `routines.json`). `validateTracks` (`src/data/validateTracks.ts`) gates uploads (dup ids = error, dup URIs = warning, plus a routine-length warning); `collectStepLibrary` (`src/data/stepLibrary.ts`) derives the distinct-move palette for the step editor. The pure merge/remove helpers are unit-tested (`routineSources.test.ts`).

The seconds-based timeline is **derived**, not stored:

- `src/data/beats.ts#beatsForStep(measures)` — one measure ("Takt") = a full 8-count, so a step is `measures × 8` beats (halves fall out: 2.5 → 20). Step lengths never shrink, so the derived timeline doesn't drift across a track. This convention is the project's domain rule — keep it in sync with `src/data/tracks.ts`'s doc comment.
- `buildCallings(steps, firstBeatSec, bpm)` accumulates beats → absolute-time `Calling[]`.
- Track metadata is **mostly fetched from Spotify**, not authored: only `id`, `spotifyUri`, and `steps` are required; `title`/`artist`/`durationMs`/`firstBeatSec`/`bpm` are optional overrides. `useTrackMeta` (player) + the pure `resolveTrackMeta` (`src/data/meta.ts`, authored-wins-over-fetched) resolve them. Title/artist/duration come from the single `/tracks/{id}` endpoint (reliable). BPM and first beat are normally **tapped in by the coach** (`src/data/calibration.ts`, `bpmFromTaps` + a "mark first beat" capture, saved to `localStorage` per URI via `useCalibration`) because Spotify's `/audio-features` and `/audio-analysis` are **deprecated and 403 for newer apps**; those endpoints are still tried as a best-effort auto-fill. Resolution priority is **authored (`tracks.ts`) > saved tap calibration > Spotify fetch > default**, folded together in `useTrackMeta`. `TrackList` resolves the whole list via `getTracksInfo`, which fans out **parallel single `getTrackInfo` calls** — the multi-get `/tracks?ids=` endpoint is **403 for newer dev-mode apps**, so don't reintroduce it.
- `PlayerScreen` memoizes `buildCallings(track.steps, meta.firstBeatSec, meta.bpm)` and feeds the `Calling[]` to `resolveCallings`. The progress bar uses the **live** duration the engine exposes (`engine.durationMs`, from the Spotify snapshot), falling back to `meta.durationMs`.
- `src/data/callings.ts#resolveCallings(callings, positionSeconds)` maps a position to current/next calling + countdown; `CallingDisplay` and the coach timeline both derive from it.

## Navigation

No router library. `src/App.tsx` switches views with local state (`list` → `ListScreen` ⇄ `player` → `PlayerScreen`, plus transient `editor`/`seed`). Two paths get real URLs via `history.pushState`/`popstate` and the pure `src/nav/routes.ts` (`parsePath`/`trackPath`/`listPath`, all **base-path aware** for sub-path deploys like GitHub Pages): `/callback` completes the OAuth exchange, and **`/track/:id`** is a track's shareable detail page. A deep link opens the player in `'view'` mode (`engine.select` — positioned but not auto-playing; press Play to start); in-app clicks open in `'start'` mode (auto-play). SPA fallback for direct deep-link loads: nginx `try_files` (Docker), the vite dev server, and on GitHub Pages a `dist/404.html` copy of `index.html` written by `scripts/make-404.mjs` (the `build` script's last step).

## Functions

Grouped by theme; unchecked entries are backlog. **Checked = built, type-checked, lint-clean, unit-tested and committed** — but Spotify-API paths are tested with mocks, so not every checked item has been verified against the live Spotify API or in a real offline browser. Caveats are noted inline.

### Authoring routines (highest leverage — today it's hand-edited TS)
- [x] **In-app step editor.** Add/edit/reorder `steps` (step, cue, measures) from the UI instead of editing the routine JSON; persist to `localStorage`. (`TrackEditor` + `tracksStore`; stored as one `tjf.tracks` list override — not per-URI; plus in-app Spotify search, step-library block-insert, BPM guidance. The **First beat (s)** field keeps a raw text buffer so an in-progress decimal like `1.0`/`1.05` isn't normalised back to `1` mid-typing — a controlled-number-input pitfall that previously blocked any value with a zero after the decimal point.)
- [x] **Import/export routines as JSON.** Back up and share the routine list without git; download/upload a file. (`RoutinesManager` + `validateTracks`. Upload now adds the file to the toggleable **"Custom routine files"** list instead of replacing the active list — see the composable-source model above.)
- [x] **Live re-time while authoring.** "Mark step boundary" tap mode during playback that fills `measures` from the music, the way first-beat capture already works. (`TapToTime` + `tapsToTiming`.)
- [x] **Validate routine vs. track length.** Warn when accumulated callings overshoot/undershoot `durationMs` so a mis-typed `measures` is caught before class. (pure `src/data/routineLength.ts#checkRoutineLength`: overshoot >1s = warn eagerly, undershoot >12s = warn; surfaced live in `TrackEditor` (uses fetched duration) and folded into `validateTracks` for the import/Validate path. Skipped for `wip` tracks.)
- [x] **Public-folder routine files.** Drop routine JSON in the web server's public folder; entries flagged `"default": true` (or named `default.json`/`default.*.json`) are **auto-enabled on first run** as the base set, and every manifest-listed file is an independently **toggleable source** (Load/Loaded) in `RoutinesManager` — including the defaults, which can be unloaded. The committed default is `public/playbook-2026.json`. (`src/data/recommendedImports.ts` + `public/routines.json` manifest + `src/data/routineSources.ts` for the merge/toggle layer; precached for offline via the PWA `globPatterns`. `tracks.ts`'s `DEFAULT_TRACKS` is kept only as the no-server/offline fallback — the `Track` *type* still lives there.)
- [x] **Mark tracks as WORK IN PROGRESS.** Flag a routine whose timings aren't finished. (`Track.wip`; editor checkbox, `WIP` badge in `TrackList`, a banner in `PlayerScreen`, suppresses the length warning, validated as a boolean.)
- [x] **Step-library autocomplete in the editor.** (The step-name inputs get a native `<datalist>` of all known moves, and when a typed name matches the library its other frequency-ranked cues/measures — previously computed by `collectStepLibrary` and discarded — appear as one-tap `.step-suggest` chips under the step row (top 3 each, current value filtered out). Applying a measures chip drops `halfPosition` back to the natural default.)
- [x] **Wire the GetSongBPM fallback into authoring.** (Extracted the player's chain into `src/beatdata/bpmLookup.ts#lookupBpm` — Deezer by ISRC first, then GetSongBPM by title/artist; never throws, unit-tested. All four call sites use it now: `useTrackMeta`, the editor's search-result annotation + recommended-BPM lookup, and `PlaylistSeed`. Results without an ISRC also get the title/artist fallback instead of an immediate null.)
- [x] **"Fit last step to track length."** (Pure `routineLength.ts#fitLastStepMeasures`, unit-tested: sizes the last step so the routine lands at the track end, snapped **down** to 0.5 so the fix never overshoots — an unreachable calling is worse than spare seconds; min 0.5; null when not computable or already fitting. The editor's length warning carries a "Fit last step (N ×8)" button that applies it.)

### Class / session flow
- [x] **Setlist mode.** Order several tracks into a session and auto-advance through them (the 20s gap already exists per-track; extend to a queue with total session time + remaining estimate). (The engine already auto-advances through its `tracks` array, so a session just launches the player with an ordered subset. `tjf.setlist` (ids, `setlistStore`) + queue toggle in `TrackList` + `SetlistPanel` (reorder/remove/total) + `App` `sessionActive` swaps the player's `tracks` to `resolveSetlist(...)`. Pure `src/data/setlist.ts#sessionEstimate` drives the total/remaining; `PlayerScreen` shows a `session` bar. `onUpdateTrack` now matches by id (not index) so tap-to-time still works on the subset.)
- [ ] **Auto-calibrate Bluetooth latency** instead of a manual slider. But how? (Assessment 2026-07: no clean client-only way exists. The PWA never hears the audio (it plays on the tablet→speaker), so it can't measure arrival time; a microphone approach — coach's phone listens for a known onset via getUserMedia and compares against the poll-interpolated position — is the only real option, but it needs mic permission, a quiet studio, and onset detection, and still folds in the ±½–1s poll jitter. The tap-based Sync offset slider effectively *is* the human version of that measurement. Revisit only if the slider proves too fiddly in class.)
- [x] **BUG: the setlist gap length never reaches the engine.** `PlayerScreen` called `usePlayerEngine(tracks, deviceId)` without a gap argument, so the countdown always ran `DEFAULT_GAP_SECONDS` (10) while `sessionEstimate` computed with the session's `gapSeconds` — countdown and remaining-time estimate could disagree. (Fixed: `PlayerScreen` now passes `session?.gapSeconds ?? DEFAULT_GAP_SECONDS` as the engine's third argument, and the auto-continue checkbox label — which hardcoded "20s gap" while the default is 10 — renders the effective value (`Auto-continue ({gapSeconds}s gap)`). Latent until now since `App` also passes `DEFAULT_GAP_SECONDS` into the session prop, but the wiring is required before any per-session gap setting. `PlayerScreen.test.tsx`'s module mock records `usePlayerEngine`'s args (and now must export `DEFAULT_GAP_SECONDS`, since `PlayerScreen` imports it from the mocked module); two new tests assert the session gap reaches the engine + label and the no-session default.)
- [x] **"Class ends at HH:MM".** (Session bar now reads "~N left · ends 18:45 · M total" — new pure `data/time.ts#formatTimeOfDay` (locale HH:MM; locale/timeZone params only for deterministic tests) applied to `Date.now() + remainingMs`, kept fresh by the ticker re-renders.)
- [x] **End-of-session screen.** (`components/player/EndedOverlay`: session mode shows "Session complete — N tracks · total" with **Restart session** (`engine.start(0)`) / **Back to list** / **Replay last track**; a single track gets "Track finished" + replay. Component-tested in `PlayerScreen.test.tsx`.)
- [x] **Allow seeking while held/gap.** (`StepTimeline` is now seekable in `gap`/`held`/`ended` too — but a raw seek there would target dead (or silent keep-awake) playback, so `engine.seekTo` re-plays the displayed track at the tapped position in those phases (`playIndex` gained an optional start position). `idle` (quiet detail view) and `loading` stay non-seekable so viewing never starts playback. Engine-tested.)
- [x] **Shareable setlists via URL.** (`/session/a,b,c` in `routes.ts` — ordered, comma-joined, per-id URI-encoded, base-path aware, round-trip tested. `startSession` pushes it, so a running session is reload-safe; `SetlistPanel` gets a Share button (Web Share API, clipboard fallback). Opening a shared link asks first (`window.confirm` — importing **replaces the coach's own saved setlist**), then imports the ids (`useSetlist.replace`; unknown ids skipped with a toast) and opens the session player in `'view'` mode — positioned on track 1, Play to start. The bare `/session` uses the saved setlist (the PWA-shortcut target). Client-only: the link carries only ids; the receiving coach needs the same routine sources.)- [x] **Volume control.** Set the Connect device's speaker volume from the phone without walking to the tablet. (`setVolume()` in `src/spotify/api.ts` → `PUT /me/player/volume`, clamped 0–100; **needs no new OAuth scope** — covered by the existing `user-modify-playback-state`. `PlaybackSnapshot.volumePercent` (from `device.volume_percent`) rides on the existing `/me/player` poll, so no extra request. The engine exposes `volumePercent`/`setVolume`: **optimistic** (slider moves instantly) + **debounced** (200ms trailing PUT so a drag isn't one request per tick), and the poller won't clobber the slider with a stale reading for ~1.5s after a change. A `Volume` icon + range slider + live `%` in `PlayerScreen`, disabled (shows `—`) until the first poll / when the device can't report a volume. Limitation: not all Connect devices support remote volume — some report `volumePercent: null` (slider disables) or 403 on write (`setVolume` throws → existing `controlFailed` toast).)

### Coach-facing display
- [x] **Stage / big-display mode.** (A `Maximize` button in the player topbar opens `.stage`: a fixed minimal-chrome takeover — the calling card stacked and centred (NOW at `--text-display`, 180px ring, cues at `--text-xl`) over the app background, plus a thin progress bar; tap anywhere to exit. Sits at z-index 5, **below** `.overlay` (10), so the gap/held/ended overlays still surface during a session. Component-tested.)
- [x] **Haptic / vibration cue** on step change (Vibration API). (`src/hooks/useHapticCue.ts`, unit-tested: 80ms buzz on a live step transition while `playing` — the initial row on mount/seek doesn't buzz, row −1 never does. iOS Safari has no Vibration API, so the "Vibrate on step change" toggle (**opt-in — default off**) only renders where `navigator.vibrate` exists — Android-only by feature detection; the downbeat pulse below is the iPhone substitute. The setting lives in a shared store (`src/data/hapticSetting.ts`: `loadHaptic`/`saveHaptic`/`hapticSupported`, key `tjf.haptic`) with **two toggles writing it**: one in the player's continue-row, one in the list-view Settings panel.)
- [x] **Mirror / left-right labeling** for cues so the coach can call mirrored to the class. (Persisted "Mirror L/R" toggle in the player (`tjf.mirror`): pure `data/mirror.ts#mirrorCue` (unit-tested) swaps left/right words in the **displayed** step names/cues — English, German (links/rechts), and bare L/R markers, case-preserving, whole-word only ("leftover" untouched). Display-only: routine data and the derived timeline are unchanged.)
- [x] **Section-coloured timeline + progress-bar step markers.** (Pure `data/sections.ts#assignSections`, unit-tested: equal cues (case/space-insensitive) share a palette index per track, wrapping over 6 CSS tones (`--section-0…5`). `StepTimeline` paints it as an inset row edge (box-shadow, no layout shift); the progress bar gets `.progress-tick` marks at each calling's start, sync-offset corrected to match the seek clock. Display-only.)
- [x] **Downbeat flash.** (`humanBeat`'s `downbeat` flag now also lifts the whole calling card: `.calling-display.downbeat` brightness pulse — 0.05s attack, 0.45s release — on the "1" of each eight. Suppressed while `announcing` (the accent frame owns that state) and under `prefers-reduced-motion`. Component-tested.)
- [x] **Keep-awake visibility during play.** (A `.keepawake-chip` pill next to the device tag shows "awake" / "awake · silent" / "awake off" and toggles on tap. `useKeepAwake` gained `setKeepAwake` (mirrors the live `setMethod` pattern): persists as a manual override — the same `saveKeepAwake` store Settings writes — and when turning off while a silent track is holding the device between tracks, pauses it immediately via `transferPlayback(play:false)`, guarded to gap/held/ended so it can never pause real music. Exposed as `engine.setKeepAwake`; engine- and component-tested.)
- [x] **Beat-grid / count visualization** (1-2-3-4-5-6-7-8) ticking with the music, driven by `bpm` + `firstBeatSec`. (The `CallingDisplay` ring is now a **human 8-count** instead of a seconds countdown: the coach counts "1 2 3 4 5 6 7 8, 2 2 3 4 5 6 7 8, …" — the first beat of every eight shows the measure number — then each step **closes with "4 3 2 →move"** (the closing counts span `BEATS_PER_COUNT`=2 beats each; the would-be "1" beat shows `→` and announces the move instead, the best-timed call). A half measure falls out as a short "N 2 3 4" group whose placement is authored per step: `StepCalling.halfPosition` = how many full measures precede the orphan 4/8 (0 = front "1 2 3 4, 1…8, …"; omitted/default = just before the close "… 4 2 3 4, 4 3 2 →"). `preCloseGroups` lays out the counted groups; the editor shows a "Half-count (4/8) is counted" select only when `measures` ends in .5 and there's a real choice; `validateTracks` checks it's an integer in `[0, floor(measures)-1]` (warns when ignored on a whole-measure step). It only affects the count display, not the derived timeline. Pure `src/data/callings.ts#humanBeat` (replaces the old `deriveCountIn`), unit-tested in `humanbeat.test.ts`; falls back to the seconds ring when no BPM. The coach timeline rows also show each step's **length in beats** under the start time (`beatsForStep`). **Ring legibility:** the measure **downbeat** (the "1" of each eight, shown as the measure ordinal) is set off in the accent colour (`humanBeat` now returns a `downbeat` flag → `.ring-num.measure`); the ring **track is shaded into the step's measures** with alternating tones (`measureTrackGradient` builds a layered `conic-gradient` — accent progress sweep over a `--ring-bg`/`--ring-bg-alt` per-measure base, so the still-to-come measures show through the unfilled arc; a short final arc falls out for a half measure); and during the closing "4 3 2 →move" the current **NOW step shrinks + dims** (`.calling-display.announcing .now .step`) so attention shifts to the announced move. `StepCalling.halfPosition` is also published in the JSON Schema (`public/routines.schema.json`).)

### Robustness & offline
- [x] **Reconnect / device-lost handling.** Surface when the target Connect device disappears or playback is hijacked by another app, with a one-tap re-target. (engine `noDevice` + `hijacked` + `recover()`, two recovery banners — detects both the device *disappearing* (empty polls) and another app *hijacking* the session (a foreign track plays for `HIJACK_POLLS` polls). Once `noDevice`, the poller **stops calling `/me/player`** (it just 204s with no device) and watches the cheaper `/me/player/devices` instead, **auto-reconnecting** (`recover()`, debounced 5s) when a device returns — so a slept tablet resumes on its own. The lost-device banner is device-agnostic ("Wake the device running Spotify…"). Unit-tested with mocks, not against a live device.)
- [x] **Token-expiry UX.** Graceful re-auth when the refresh fails mid-class rather than a silent stall. (`AUTH_EXPIRED_EVENT` → login screen; plus `startTokenAutoRefresh` proactive background refresh.)
- [x] **Premium gate.** Detect a non-Premium account and warn clearly instead of letting Play/Pause/Seek fail silently. (`getCurrentUser()` → `GET /me` returns `{ displayName, product }`; needs the **new `user-read-private` scope** added to `SCOPES` in `config.ts` — existing tokens lack it, so `product` comes back null until the user re-logs-in and re-consents, which leaves the gate **inconclusive (no banner)** rather than wrong. `App` fetches the profile once after login (best-effort, `online`-gated), greets by `displayName` in the topbar (`.topbar-user`), and shows a `.premium-banner` warning in the list view when `product != null && product !== 'premium'`. It's a **soft gate** — search/authoring still work; only the playback controls need Premium. Cleared on logout.)
- [x] **Offline audit.** Verify the shell, track list, and authored callings truly work offline (matches the stated offline-first goal). (`useOnline` banner + gated fetches; precache manifest **inspected** — not yet confirmed in a live offline browser session.)
- [x] **PWA install prompt / affordance.** (`useInstallPrompt` captures `beforeinstallprompt`; `InstallPrompt` banner in the list view offers one-tap install on Android/desktop, "Add to Home Screen" guidance on iOS, hidden once installed or dismissed (`tjf.installDismissed`).)
- [x] **Fast loading search insert bar.** Hard code some search results and display them opening a search without text for quick access the most popular 20 jumping fitness songs offline. (`src/data/popularTracks.ts` quick-picks rendered in `TrackEditor` when the search is empty and no track is set yet — no network call. **Full 20 curated jumping-fitness tracks**, IDs resolved via the `spotify-search` skill (`scripts/spotify-search.mjs`); BPMs are approximate starting points.)
- [x] **Smoother progress/countdown ring.** (`CallingDisplay` ring `conic-gradient` now uses 2-decimal precision instead of whole percents, so it sweeps each 100ms tick.)

### Track list UX
- [x] **Search / filter / favorites** in `TrackList` as the list grows. (search box + `useOnline`-aware filtering, starred favorites in `tjf.favorites`.)
- [x] **Seed tracks from a Spotify playlist** (one call to import URIs, then author steps). (`PlaylistSeed` → routine stubs with BPM-if-known.)
- [x] **Tidy track titles in the list.** Strip unimportant trailing qualifiers ("Radio Edit", "Extended Mix", "2011 Remaster", "Single Version", …) and mark the cut with an ellipsis. (pure `src/data/title.ts#cleanTitle`, unit-tested: peels a trailing `(...)`/`[...]` or ` - …` segment whose words match a noise regex, appends `…`; conservative — keeps "Remix"/"feat."/"Live" and mid-title words; full title stays in the `track-title` `title` tooltip. Used by `TrackList` for display only.)

### Engineering / quality
- [x] **Component tests** for `PlayerScreen` gap/held overlays and `CallingDisplay`. (`CallingDisplay.test.tsx` renders the pure display across states — NOW/NEXT, the no-current placeholder, "End of track", the seconds fallback vs. the BPM-driven count-in/"CALL NOW" announce, and the proportional ring fill. `PlayerScreen.test.tsx` mocks the four hooks it depends on (`usePlayerEngine`/`useTrackMeta`/`useCalibration`/`useWakeLock`) and drives a controlled engine to assert the **gap** overlay (countdown, +5s/Hold/Start now → `extendGap`/`holdNow`/`skipGap`) and **held** overlay (Continue → `skipGap`, "back to list" → `onBack`) render and wire correctly, plus their absence during normal playback. Both use `@testing-library/react` with plain assertions (no jest-dom matchers, so no setup file/type augmentation needed); the PlayerScreen test stubs `localStorage` since jsdom's opaque origin has none.)
- [x] **Wire up ESLint in CI** (config was added; make it run on PRs). (`.github/workflows/ci.yml` runs typecheck/lint/test/build on push + PR.)
- [x] **Revisit track-end heuristic** (`duration − 500ms`) — reframed from "configurable" to *stability*: kept the 500ms (it's a needed pre-empt lead, not a tuning knob) and left repeat/shuffle untouched; hardened the boundary so a missed pre-empt within 1.5s of the end is still treated as the end, not a false "hijack"/replay: both a foreign track (auto-advance) and the same track restarting from ~0 (`repeat = track` loop) call `enterGapOrEnd`. (`END_AUTOPLAY_WINDOW_MS` / `LOOP_RESTART_MS` in `usePlayerEngine`.)
- [x] Look on error handling like .catch(). Are they handled or empty? Add a main error way for example by using a little very easy toast. (Audited every `.catch()`/`try` site: component-local errors already surface their own UI (`App` authError/infosError, `PlaylistSeed`, `RoutinesManager`, `engine.error` → `PlayerScreen`); best-effort fetches (recommended/default routines, resume check, metadata/BPM auto-fill, wake-lock release, install prompt) stay intentionally silent. The genuine gap was the **user-initiated playback controls failing silently** — `togglePlayPause` (pause/resume), `seekTo`, `holdNow` were all `.catch(() => {})`, so a failed pause/resume/seek left the coach with no feedback. Added a small app-wide toast as the **main error channel**: `src/data/toast.ts` (module-level store: `toast()`, `subscribe`, `getToasts`, `dismissToast`, 5s auto-dismiss) + `src/components/Toaster.tsx` (`useSyncExternalStore`, tap-to-dismiss, mounted once in `App`). The engine routes those control failures through it via `controlFailed(what)`; `enterGapOrEnd`'s automatic cleanup pause stays silent (the gap/end proceeds regardless). Toast store unit-tested (`toast.test.ts`).)
- [x] Shwo volume bar only if it is gonna work. (`VolumeSlider` returns null while `volumePercent == null` — a null reading means the device can't report (and usually can't accept) remote volume, so the permanently disabled "—" slider is gone; it appears once the first poll delivers a value. A write-403 after that still surfaces via the existing `controlFailed` toast.)
- [x] **Gate the `.debug` line.** (Wrapped in `import.meta.env.DEV` — dev server/tests only, stripped from production builds.)
- [x] **Drop the `user-read-currently-playing` scope.** (Removed from `SCOPES` with a comment; current-track reads ride `GET /me/player` (`user-read-playback-state`). Smaller consent screen; existing tokens unaffected.)

### Features
- [ ] Find an API to parse "First beat (s)" from song. Maybe there is a free pass (10 querys per day we can cache and use over some days to save money) (Assessment 2026-07: no known free web API returns beat-grid/first-beat — Spotify's `/audio-analysis` is the 403'd one, Deezer/GetSongBPM only do tempo. Realistic candidates: a paid analysis API, or client-side onset detection over a decoded preview clip (Deezer 30s previews + an autocorrelation pass in a worker) — a project of its own. Tap calibration remains the working answer.)
- [x] **Web Share API for track links.** (The player's link button uses `navigator.share` where it exists — iOS Safari 14.5+/Android, labelled "Share this track" — and falls back to the old clipboard copy elsewhere. Share-sheet rejection (user closed it) is swallowed.)
- [x] **Manifest `shortcuts`** (+ `share_target` assessed). (One shortcut — "Start setlist session" → the relative URL `session`, resolving against the manifest scope so sub-path deploys work; opens the saved setlist positioned to play (see the shareable-setlists entry). iOS ignores shortcuts harmlessly. **`share_target` deliberately skipped**: it only works on installed-PWA Android, and this project's coach device is an iPhone — revisit if an Android coach base appears.)
- [x] Add url path for view of song detail page. (`/track/:id` via `src/nav/routes.ts` + `pushState`/`popstate` in `App`; opens the player in `'view'` mode via `engine.select`, with a "Copy link" button. Base-path aware. See the Navigation section.)
- [x] Use pen icon for edit button (The per-track "Edit" text button in `TrackList` is now a `Pen` icon (new in `icons.tsx`, Lucide pencil) with an `aria-label`/`title` "Edit routine"; `.track-edit` centres it. The same `Pen` icon also sits **top-right of the player/detail-page topbar** (`PlayerScreen` `onEdit` → `App.openEditor`, resolving the track by id), so a deep-linked `/track/:id` view can jump straight into the editor.)
- [x] Scroll position resets to the top on every screen change (`App` `useEffect` on `view`/`selectedIndex` → `window.scrollTo(0, 0)`), so opening a track, returning to the list, or following a deep link no longer inherits the previous screen's scroll.
- [x] Playbook tracks no longer author `firstBeatSec: 0` — an authored value (even `0`) disables `CalibrationPanel`'s "Mark first beat", so the 26 zero entries were stripped from `public/playbook-2026.json` (the real `1`/`1.3`/`0.85` kept). The tracks now fall back to the default `0` *and* let a coach capture the first beat by tapping.
- [x] Let spotify app in background (save device) never go to sleep (maybe play a "empty" song instead of pausing?) (Web-API **keep-alive ping** rather than a silent track. While idle (`paused`/`gap`/`held`/`ended`) the engine re-asserts the device every `KEEP_AWAKE_MS` (15s) via `transferPlayback(id, false)` — no audio; the transfer also revives a device that had merely gone idle. **Gated to "this same machine"** by a user-agent → device-name heuristic (`src/spotify/localDevice.ts#isLikelyLocalDevice`, unit-tested): a browser can't read its own OS device name, so we pull hints from `navigator.userAgent` (the Android model the UA embeds, e.g. "Pixel 7"/"SM-X700", and its first token; or the "iPhone"/"iPad" keyword) and match a Spotify device whose name contains one. As a **secondary fallback** (when the name yields no hint — e.g. a privacy-frozen Android UA reporting the model as "K"), the device `type` from `/me/player` (`PlaybackSnapshot.deviceType`, "Smartphone"/"Tablet"/"Computer"/…) is matched against the UA's platform (`deviceTypesFromUserAgent`). `type` only ever influences the keep-awake *default*; the ping always targets the exact device we last played on, so a loose type match can't keep a stranger's device awake. The keep-alive interval **re-polls `getDevices()`** and matches the last-played device there (the main poller is gated to `playing`/`paused`, so it's stale while held). Limitation: still weak/none on a desktop whose Spotify name is its hostname. Lives in its own focused hook **`src/hooks/useKeepAwake.ts`** (owns the setting + the ping loop), composed by `usePlayerEngine` which feeds it `phaseRef`/`hijackedRef`/`deviceNameRef` and calls `syncDefault(deviceName, deviceType)` from the poller. It's a **coach setting** living in a shared store (`src/data/keepAwakeSetting.ts`), modelled as **two booleans** rather than a tri-state so the UI is a plain on/off toggle that still remembers auto-vs-override: `tjf.keepAwakeManual` (has the coach chosen?) + `tjf.keepAwake` (the value). A single on/off toggle in the list-view **Settings** panel (next to the GetSongBPM key) is the only writer — `saveKeepAwake(bool)` marks it manual; until then `loadKeepAwake()` shows the auto default (on). The engine's `useKeepAwake` reads `loadKeepAwakeOverride()` (= the value when manual, else null) once at mount; `engine.keepAwake` (read-only, for the held overlay's "Keeping <device> awake") is the effective on/off. The **default follows the heuristic** (on for our own device, off otherwise), but an explicit choice wins and is remembered — so a coach can force it on when the heuristic fails (frozen UA) or off when not wanted. The keep-alive targets the device we last played on **by name** (re-resolving its current id via `getDevices`, since Spotify reassigns ids), only while `idle` and not `hijacked`. The held overlay shows "Keeping <device> awake". The mock in `usePlayerEngine.test.ts` gained `transferPlayback`/`getDevices` and stubs `navigator.userAgent`. **Two keep-awake *methods*** now exist (`KeepAwakeMethod` in `keepAwakeSetting.ts`, persisted at `tjf.keepAwakeMethod`): the default **`'ping'`** (the no-audio `transferPlayback` above) and **`'silent'`**, which instead **plays a silent track** (`playTrack(silentUri, deviceId)`) to also keep a **Bluetooth speaker connected** (it drops after a pause). Silent mode runs **only between tracks** (`gap`/`held`/`ended`) — never on a mid-track `paused`, which would lose the resume position, so there it falls back to the ping. The silent-track URI is overridable (`tjf.silentTrackUri`, `loadSilentTrackUri`) and defaults to `DEFAULT_SILENT_TRACK_URI` ("Silence 10 Minutes", a 10-min silent track). `useKeepAwake` reads the method + URI once at mount (like the override); the Settings panel exposes the method as a nested toggle (+ URI field, shown only when keep-awake is on). Unit-tested in `usePlayerEngine.test.ts` (plays the silent track when held; still pings on a mid-track pause). Because silent mode makes Spotify show "Silence …" as now-playing, the coach is **told what's happening**: a **one-time toast** (`tjf.silentNoticeShown`, fired from `useKeepAwake` the first time a silent track ever plays) explains it points to Settings; and the **held overlay** shows a persistent line ("Playing a silent track on <device> to keep it ready — Spotify shows it as 'now playing'") with a **"Stop the silent track"** link. That link flips the method **live** — `useKeepAwake` now holds `method` in state with a `setMethod` that persists *and* re-asserts the device immediately (play:false) so the silent track pauses at once, not on the next 15s tick; surfaced through the engine as `keepAwakeMethod` / `setKeepAwakeMethod`. **Closing the pre-play disconnect gaps** (so a device is warm before the *first* track, not just between tracks): (1) the engine's `useKeepAwake` now also fires in the player's pre-play **`idle`** phase (a deep-linked `/track/:id` detail view) — gated default-on (explicit-off wins) and targeting the **selected `deviceId`** when no device name has been learned yet (`deviceIdRef` passed in; `'loading'` stays excluded so a transfer can't cancel the play we just sent); (2) a new **app-level `src/hooks/useDeviceKeepAwake.ts`** (mounted in `App`, gated to `loggedIn && online && view !== 'player'`) pings the device every 15s on the **list/editor/seed** screens, where the engine isn't mounted at all — it targets **only the device the coach explicitly selected** in the picker (no active-device / `isLikelyLocalDevice` guessing here: keeping a device awake *seizes* it on Connect, so we never grab one the coach didn't choose; no selection → it does nothing), and re-reads `loadKeepAwake()` each tick. The two never overlap: the player view owns the device (`view === 'player'`), every other screen is the app-level loop's. Unit-tested (`useDeviceKeepAwake.test.ts`; engine idle cases in `usePlayerEngine.test.ts`). **On-enter kickoff:** an effect in `usePlayerEngine` fires one keep-alive `KEEP_AWAKE_KICKOFF_MS` (1s) after entering an idle/between-tracks phase, so the silent track / ping starts within ~1s of a track ending (or of opening a detail view) instead of waiting up to a full 15s tick; the 1s settle lets `enterGapOrEnd`'s fire-and-forget pause land first so it can't race ahead and pause the silent track we're starting. The steady-state interval is unchanged, and the app-level `useDeviceKeepAwake` already pings immediately when it activates. Tested (silent track starts well before the 15s interval). **Hotfix — never pause a live track:** `useDeviceKeepAwake` first checks `getPlaybackState()` and **skips the ping when `isPlaying`** — otherwise `transferPlayback(id, false)` would PAUSE the current song the moment the coach left a playing track and tapped back to the list. A playing device is already awake, so there's nothing to do. Tested (`useDeviceKeepAwake.test.ts`: no transfer while playback is active).)

### Production ready
- [x] Is the nginx container image CORS etc. ready? (`nginx.conf.template` is an nginx env-template: opt-in CORS via `-e CORS_ALLOW_ORIGIN=<origin|*>` → `Access-Control-Allow-Origin` (empty default = no header, so cross-origin reads stay blocked). The app itself needs no CORS — same-origin static files; Spotify/Deezer enforce their own (Deezer→JSONP). Also hardened: CSP tuned to real loads (self scripts, Deezer JSONP, inline-style ring, Spotify connect-src), `X-Content-Type-Options`/`Referrer-Policy`/`X-Frame-Options`/`Permissions-Policy`, `application/manifest+json`. Built image verified with podman: default → no ACAO; `-e CORS_ALLOW_ORIGIN='*'` → header present; security headers inherited across `/`, deep links, sw.js, manifest, assets.)
- [x] performance / caching used? (Audit 2026-07: the layers were already mostly there — SW precache covers shell + routine JSON (`globPatterns`), immutable track metadata is two-layer cached (`spotify/metaCache.ts`: memory + `localStorage`, only successes stored so 403s retry), and `getTracksInfo` fans out parallel single-track calls. The one gap was **album art**: added a Workbox runtime `CacheFirst` for `i./mosaic.scdn.co` (bounded: 100 entries / 30 days) so covers render instantly and offline. Spotify API calls stay deliberately uncached — live playback state. No further caching worth adding client-side.)
- [x] Real landing page for new user with function list and cost explaination (ko-fe). Link to source code too! Issues too. (`LoginScreen` is now the landing page: feature list, "what it costs" (free/open-source, Premium needed, Ko-fi support), and footer links to source/issues/Ko-fi. URLs in `src/links.ts`.)
- [x] Add a better html landing page as just "BPM Data getsongbpm credits". It is shown for 2 seconds before javascript is loaded so i matters.
- [x] Add impress page and cookie banner to footer. (German Impressum + privacy page as a native `<dialog>` modal — `LegalModal` opened from a footer "Impressum & privacy" link; contact/address details live in `src/legal.ts` (§ 5 DDG). Plus `CookieNotice`: a one-time dismissible storage notice (`tjf.storageNoticeDismissed`) — informational, not a consent gate, since only strictly-necessary functional `localStorage` is used (no tracking/analytics/third-party cookies). Both hosted by `SiteFooter` so they appear on every screen incl. the pre-login landing.)
- [x] Recommended routines json are not working on github pages but locally. (`src/data/recommendedImports.ts` hard-coded root-absolute paths (`/routines.json`, manifest `"/playbook-2026.json"`) that ignored the deploy base, so a sub-path deploy (`base: /CueSheet/`) 404'd the JSON and silently fell back to the built-in `DEFAULT_TRACKS`. Made the module base-path aware like `routes.ts`: `toUrl(file, base)` joins onto `import.meta.env.BASE_URL` (leading `/` treated as base-relative, absolute http(s) URLs untouched); `loadRecommendedRoutines`/`loadDefaultRoutines`/`fetchRoutineFile` take a `base` param defaulting to `BASE_URL`. The failure was silent because default-routine loading swallows errors by design (additive/offline-first).)
- [x] Fix the "Copy to tracks.ts" button, what should this be doing? (Removed it. It was a dev shortcut that copied a `bpm: …, firstBeatSec: …,` TS fragment to paste into `tracks.ts`, but routines no longer live there — they're JSON in `public/` + the in-app editor — and tap calibration already persists per-URI in `localStorage`, so the button was obsolete and misnamed. Dropped the button plus the `copyToData` helper and `copied`/`copyData` state in `PlayerScreen`.)
- [ ] Why do we need a "Tap-to-time the steps" button and a "Tap tempo" button. We can merge that right? (Survey answer: they are different tools — don't merge the math, merge the *entry point*. Tap tempo (`CalibrationPanel` → `bpmFromTaps`) derives a BPM from wall-clock `performance.now()` taps and writes to the per-URI `localStorage` calibration; Tap-to-time (`TapToTime` → `tapsToTiming`) *requires* a known BPM and converts playback-position taps into per-step `measures` + `firstBeatSec`, written into the routine JSON. Different clocks, different math, different stores; the only true overlap is first-beat capture (both record one, into different places). A single "Timing" panel with a guided flow — no BPM? tap tempo first; BPM known? offer step timing — would fix the UX confusion without conflating the mechanisms.)
- [x] On iphone there is still a horiziontal scroll possible (Root cause: flex/grid children with the default `min-width: auto` couldn't shrink, so a long track title — and the editor's step inputs — forced their row wider than the viewport. Fixed at the source: `min-width: 0` on `.track-meta` (+ title/artist ellipsis truncation, `nowrap` badges) and on `.editor input`. Safety net: switched the page-level guard from `overflow-x: hidden` to `overflow-x: clip` on `html`/`body`/`#root` — `clip` creates no scroll container, so iOS Safari can't rubber-band/pan sideways even in **standalone (home-screen) PWA mode**, where `hidden` was unreliable. Inner `overflow-x: auto` blocks keep their own scroll.)
- [x] On iphone there is a bug that different row widths in Track List. So the buttons are not in one grid. (Same root cause as above — a long title with `min-width: auto` overran the row and shoved the right-hand badges/Edit button to different positions per row. `min-width: 0` on `.track-meta` + ellipsis truncation keeps every row the container width, so the badges/time/Edit column lines up. `.track-aside` is `flex-shrink: 0` so it never collapses.)
- 